pipeline {
    agent any 

    stages {
        stage('Build') { 
            steps { 
                	withMaven(maven : 'hhman') {
			sh 'mvn clean' 
                                                   }
                   }
                        }
stage('Test'){
            steps {
                withMaven(maven : 'hhman') {
		sh 'mvn test'
                			   }
                  }
              }
        
	    }
	}