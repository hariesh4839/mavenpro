pipeline {
    agent any 

    stages {
        stage('Build') { 
            steps { 
                	withMaven(maven : 'hhman') {
			sh 'mvn clean' 
                                                   }
                   }
                        }
        
	    }
	}